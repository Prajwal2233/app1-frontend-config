apiVersion: v1
kind: Service
metadata:
  name: app1-frontend-svc
spec:
  type: NodePort
  selector:
    app: app1-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 31001
